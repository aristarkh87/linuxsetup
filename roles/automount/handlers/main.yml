---
- name: Restart mounts
  become: yes
  systemd: 
    name: "{{ mount_directory | basename }}-{{ item }}.automount"
    daemon_reload: yes
    enabled: yes
    state: restarted
  with_items: "{{ shares }}"
